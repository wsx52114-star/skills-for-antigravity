interface:
  display_name: "Build"
  short_description: "Implement one approved vertical slice with evidence"
  default_prompt: "Use $build to implement the next approved vertical slice."
policy:
  allow_implicit_invocation: false
