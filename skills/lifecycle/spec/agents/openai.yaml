interface:
  display_name: "Spec"
  short_description: "Define an approved specification before implementation"
  default_prompt: "Use $spec to define and approve a specification for this change."
policy:
  allow_implicit_invocation: false
