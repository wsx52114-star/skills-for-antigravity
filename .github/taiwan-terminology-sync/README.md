# Taiwan.md terminology synchronization

This fork-owned control plane normalizes `data/terminology/*.yaml` from
`frank890417/taiwan-md` into the pinned snapshot used by
`skills/language/taiwan-term/`.

Updates are proposed through review-only pull requests. Runtime audits never
fetch the moving upstream branch.
